{
  "name": "meal-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Meal planning app with admin recipe management and shopping list generation",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
